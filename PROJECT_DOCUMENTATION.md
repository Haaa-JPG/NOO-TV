# NOO TV - منصة البث العربية

## دليل شامل للمشروع

---

## نظرة عامة

NOO TV هي منصة بث فيديو عربية متكاملة بتصميم Netflix، مبنية بتقنيات حديثة وتوفر تجربة مشاهدة احترافية. المنصة تدعم الأفلام والمسلسلات مع واجهة عربية كاملة (RTL) ونظام إدارة محتوى متقدم.

### التقنيات الأساسية

| التقنية | الاستخدام |
|---------|-----------|
| Next.js 14 | إطار العمل الرئيسي للواجهة الأمامية والخوادم |
| React 18 | مكتبة بناء واجهات المستخدم |
| Supabase | قاعدة البيانات والمصادقة |
| Tailwind CSS | التنسيق والتصميم |
| Playwright | استخراج روابط البث تلقائياً |
| WebTorrent | توصيل الفيديو عبر P2P |
| HLS.js | تشغيل بث HLS direct |
| PostgreSQL | قاعدة البيانات الأساسية |

---

## الميزات الرئيسية

### 1. الصفحة الرئيسية (Netflix Style)

- **بانر رئيسي (Hero Banner):** عرض محتوى مميز مع خلفية فيديو أو صورة يتغير تلقائياً كل 8 ثوانٍ
- **شريط أفقي للأفلام:** عرض الأفلام الأكثر مشاهدة في شريط أفقي قابل للتمرير
- **شريط أفقي للمسلسلات:** عرض المسلسلات حسب التصنيف
- **تصنيفات:** تصفح المحتوى حسب التصنيف (أكشن، دراما، كوميديا، إلخ)
- **بحث فوري:** بحث سريع في جميع المحتوى
- **عداد المشاهدات:** عرض عدد المشاهدات على كل بطاقة

### 2. مشغل الفيديو

- **دعم شامل للروابط:**
  - YouTube (مع إخفاء العلامات التجارية)
  - Vimeo
  - Dailymotion
  - ملفات MP4 مباشرة
  - روابط HLS m3u8
  - كود iframe مدمج

- **ميزات المشغل:**
  - تشغيل P2P صامت في الخلفية (WebTorrent)
  - استئناف المشاهدة من حيث توقفت
  - فيديو تعريفي قبل كل محتوى
  - تحكمات فيديو مخصصة بتصميم زجاجي (glassmorphism)
  - دعم الشاشات اللمسية (double-tap للتقديم)
  - التحكم بالصوت بالسحب
  - سرعة التشغيل (0.25x إلى 2x)
  - ملء الشاشة بالكامل

### 3. صفحات المحتوى

**صفحة الأفلام:**
- عرض сетка (grid) لجميع الأفلام
- فلترة حسب التصنيف
- عرض التقييم والمشاهدات
- شارة الدبلجة والترجمة

**صفحة المسلسلات:**
- عرض جميع المسلسلات مع عدد المواسم
- فلترة حسب التصنيف
- شارة يوم العرض

**صفحة تفاصيل المسلسل:**
- بانر سينمائي مع خلفية فيديو (YouTube أو MP4)
- معلومات المسلسل (التصنيف، التقييم، المشاهدات)
- وصف قابل للتوسيع (3 أسطر + زر المزيد)
- أزرار "بدء المشاهدة" و"إضافة للمفضلة"
- دعم روابط Trailer مع تحديد وقت البداية والنهاية

**صفحة المشاهدة:**
- مشغل فيديو متكامل
- قائمة حلقات قابلة للتمرير
- تتبع سجل المشاهدة
- عرض الحلقة التالية

### 4. لوحة التحكم الإدارية

**تبويب الأفلام:**
- إضافة/تعديل/حذف أفلام
- دعم روابط YouTube, MP4, iframe
- رفع حلقات عبر JSON
- معاينة الفيديو في نافذة منبثقة
- حذف جميع المشاهدات
- عرض حالة انتهاء صلاحية الروابط

**تبويب المسلسلات:**
- إضافة/تعديل/حذف مسلسلات
- إدارة المواسم والحلقات
- رابط Trailer مع وقت البداية والنهاية (mm:ss)
- اختيار يوم العرض (أحد إلى خميس)
- رفع حلقات عبر JSON

**تبويب البانر الرئيسي:**
- إدارة عناصر البانر (Netflix style)
- إضافة فيديو أو صورة مع وقت البداية والنهاية
- ربط المحتوى بالمسلسل أو الفيلم
- حفظ بالثوانٍ (mm:ss)

**تبويب المستخدمين:**
- إنشاء حسابات جديدة
- حذف مستخدمين (يحذف من auth.users وpublic.users)
- حظر/إلغاء حظر المستخدمين
- عرض إحصائيات (عدد المستخدمين، المشاهدات، الإعلانات)

**تبويب الإعدادات:**
- إعداد الفيديو التعريفي (intro video)
- رابط فيديو التعريف + وقت البداية والنهاية
- إعدادات الموقع العامة

**تبويب الشكاوى:**
- عرض الشكاوى المرسلة من المستخدمين
- معدل إرسال: 3 شكاوى كل 5 دقائق

### 5. لوحة تحكم المستخدم

- **سجل المشاهدة:** بطاقات مصغرة مع شريط تقدم
- **إضافة للمفضلة:** حفظ الأفلام والمسلسلات
- **حذف من السجل:** حذف عناصر محددة
- **إعادة تعيين:** حذف جميع المشاهدات

### 6. الصفحات الثابتة

- **الخصوصية:** سياسة الخصوصية
- **إخلاء المسؤولية:** إخلاء المسؤولية القانونية
- **الشكاوى:** نموذج إرسال الشكاوى
- **الفئات:** تصفح المحتوى حسب التصنيف

### 7. نظام الإشعارات

- إشعارات للمحتوى الجديد
- إشعارات للحلقات الجديدة
- إدارة الإشعارات من لوحة التحكم

### 8. PWA (تطبيق موبايل)

- تثبيت التطبيق على الهاتف والكمبيوتر
- أيقونة مخصصة
- وضع شاشة كاملة

---

## هيكل الملفات

```
NOO-TV/
├── app/                          # صفحات Next.js
│   ├── page.js                   # الصفحة الرئيسية
│   ├── layout.js                 # التخطيط العام (Header + Footer + BanModal)
│   ├── sitemap.js                # خريطة الموقع الديناميكية
│   ├── admin/
│   │   └── page.js               # لوحة التحكم الإدارية
│   ├── api/
│   │   ├── admin-users/route.js  # إدارة المستخدمين
│   │   ├── auth-direct/route.js  # تسجيل حساب مباشر (PostgreSQL)
│   │   ├── complaints/route.js   # إرسال الشكاوى
│   │   ├── cors-proxy/route.js   # بروكسي CORS للفيديو
│   │   ├── intro/route.js        # جلب فيديو التعريف
│   │   └── proxy/route.js        # بروكسي الفيديو العام
│   ├── auth/
│   │   └── page.js               # صفحة تسجيل الدخول
│   ├── categories/
│   │   └── page.js               # صفحة التصنيفات
│   ├── complaints/
│   │   └── page.js               # صفحة الشكاوى
│   ├── disclaimer/
│   │   └── page.js               # إخلاء المسؤولية
│   ├── movies/
│   │   └── page.js               # صفحة الأفلام
│   ├── privacy/
│   │   └── page.js               # سياسة الخصوصية
│   ├── search/
│   │   └── page.js               # صفحة البحث
│   ├── series/
│   │   ├── page.js               # قائمة المسلسلات
│   │   └── [id]/
│   │       ├── page.js           # تفاصيل المسلسل (server)
│   │       └── series-detail-client.js  # تفاصيل المسلسل (client)
│   ├── user/
│   │   └── page.js               # لوحة تحكم المستخدم
│   └── watch/
│       ├── movie/[id]/
│       │   ├── page.js           # مشاهدة الفيلم (server)
│       │   └── movie-watch-client.js  # مشاهدة الفيلم (client)
│       └── series/[id]/
│           ├── page.js           # مشاهدة الحلقة (server)
│           └── series-watch-client.js  # مشاهدة الحلقة (client)
├── components/                   # مكونات React
│   ├── ban-modal.jsx             # نافذة الحظر
│   ├── footer.jsx                # التذييل
│   ├── p2p-video-player.jsx      # مشغل P2P
│   ├── pwa-install.jsx           # تثبيت PWA
│   ├── video-player.jsx          # المشغل الرئيسي
│   └── ui/                       # مكونات shadcn/ui (30+ مكون)
├── hooks/                        # خطافات مخصصة
│   └── use-toast.js              # نظام الإشعارات
├── lib/                          # المكتبات المساعدة
│   ├── db.js                     # اتصال PostgreSQL
│   ├── rate-limit.js             # تحديد المعدل
│   ├── security.js               # تنظيف المدخلات
│   ├── supabase.js               # عميل Supabase
│   └── utils.js                  # أدوات مساعدة
├── public/                       # الملفات العامة
│   ├── manifest.json             # ملف PWA
│   ├── robots.txt                # قواعد الم蒴نة
│   └── sw.js                     # Service Worker
├── scripts/                      # خدمات الخلفية
│   ├── cron.js                   # تحديث الروابط تلقائياً
│   ├── scraper.js                # استخراج الروابط بـ Playwright
│   └── worker.js                 # معالج الاستخراج
├── server.js                     # الخادم الموحد (Next.js + Worker + Cron)
├── middleware.js                  # وسيط Next.js
├── next.config.js                # إعدادات Next.js + CSP
├── tailwind.config.js            # إعدادات Tailwind
├── supabase_production_schema.sql # قاعدة البيانات الموحدة
├── BUYER_GUIDE.md                # دليل المشتري
├── render.yaml                   # إعدادات Render
├── Dockerfile                    # ملف Docker
└── package.json                  # الحزم
```

---

## قاعدة البيانات

### الجداول (15 جدول)

| الجدول | الوصف |
|--------|-------|
| `users` | المستخدمون (يمتد auth.users) |
| `movies` | الأفلام مع حالة البث وانتهاء الصلاحية |
| `series` | المسلسلات مع رابط Trailer |
| `seasons` | المواسم |
| `episodes` | الحلقات مع مصدر الرابط وحالة البث |
| `categories` | التصنيفات |
| `watchlist` | قائمة المفضلة |
| `watch_history` | سجل المشاهدة مع وقت المشاهدة |
| `ratings` | التقييمات (1-5) |
| `comments` | التعليقات |
| `user_notifications` | إشعارات المستخدمين |
| `ads` | الإعلانات |
| `site_settings` | إعدادات الموقع (key-value) |
| `jobs` | طابور الأعمال للاستخراج |
| `worker_heartbeat` | صحة العامل الخلفي |

### الدوال (12 دالة)

| الدالة | الوصف |
|--------|-------|
| `is_admin()` | فحص صلاحية المدير |
| `direct_signup()` | إنشاء حساب مباشر عبر PostgreSQL |
| `handle_new_user()` | إنشاء ملف شخصي تلقائياً عند التسجيل |
| `sync_user_profile()` | مزامنة الملف الشخصي |
| `increment_movie_views()` | زيادة عداد مشاهدات الفيلم |
| `increment_series_views()` | زيادة عداد مشاهدات المسلسل |
| `increment_episode_views()` | زيادة عداد مشاهدات الحلقة |
| `claim_next_job()` | المطالبة بالعمل التالي (ذري) |
| `complete_job()` | إكمال العمل |
| `fail_job()` | فشل العمل مع إعادة محاولة |
| `recover_stale_jobs()` | استرداد الأعمال العالقة |
| `update_worker_heartbeat()` | تحديث صحة العامل |

### الأمان (RLS)

- **المستخدمون:** يرون ملفهم الشخصي فقط، المديرون يدركون الجميع
- **الأفلام:** العامة تُرى، المديرون يدركون/يعدلون/يحذفون
- **المسلسلات:** العامة تُرى، المديرون يدركون/يعدلون/يحذفون
- **الحلقات:** العامة تُرى، المديرون يدركون/يعدلون/يحذفون
- **المفضلة:** كل مستخدم يرى مفضلته فقط
- **السجل:** كل مستخدم يرى سجله فقط
- **التعليقات:** المعتمدة تُرى للجميع
- **الإعدادات:** العامة للقراءة، المديرون للتعديل

---

## الخدمات الخلفية

### العامل (Worker)

- يفحص الطابور كل 5 ثوانٍ
- يستخرج روابط HLS من صفحات المصدر
- يدعم إعادة المحاولة (3 محاولات)
- يتعامل مع الأعمال العالقة تلقائياً

### المجدول (Cron)

- ي-cycle كل ساعتين (قابل للتعديل)
- يتحقق من انتهاء صلاحية الروابط
- يعيد استخراج الروابط القريبة من الانتهاء
- يدعم التوازي (5 أعمال في نفس الوقت)

### بروكسي الفيديو

- **CORS Proxy:** ي mediate طلبات CORS للفيديو
- **General Proxy:** يعيد كتابة ملفات m3u8
- **Service Worker:** يتزوير Referer/Origin على الجهاز
- **مدة الانتهاء:** 15 ثانية (CORS) / 30 ثانية (عام)

---

## الأمان

### حماية OAuth

- JWT verification في middleware
- فحص انتهاء الصلاحية
- إعادة توجيه للمصادقة

### حماية المدخلات

- تنظيف XSS مع `sanitizeText()`
- التحقق من البريد الإلكتروني `isValidEmail()`
- التحقق من UUID `isValidUUID()`
- تشفير HTML entities

### حماية API

- تحديد المعدل (Rate Limiting)
- فحص حجم الطلب (10MB max)
- فحص صلاحيات المدير في كل طلب
- مفتاح SERVICE_ROLE للم Operations

### حماية الشبكة

- CSP headers مع `frame-ancestors 'none'`
- HSTS مع preload
- X-Frame-Options: DENY
- X-Content-Type-Options: nosniff
- Referrer-Policy: strict-origin-when-cross-origin

### Service Worker

- تزوير Referer/Origin للفيديو
- CORS headers للطلبات
- لا يحذف أمان upstream

---

## النشر

### Render

- **Build:** `npm install && npx playwright install --with-deps chromium && npm run build`
- **Start:** `node server.js`
- **الخادم:** يبدأ على `127.0.0.1` (Render proxy)

### البيئة المطلوبة

```
NEXT_PUBLIC_SUPABASE_URL        # رابط مشروع Supabase
NEXT_PUBLIC_SUPABASE_ANON_KEY   # مفتاح Supabase العام
SUPABASE_URL                    # رابط Supabase (للخادم)
SUPABASE_SERVICE_ROLE_KEY       # مفتاح الدور الخدمي
DATABASE_URL                    # رابط PostgreSQL
ENABLE_WORKER=true              # تفعيل العامل
ENABLE_CRON=true                # تفعيل المجدول
CRON_SCHEDULE=0 */2 * * *       # جدول المجدول
WORKER_CONCURRENCY=1            # توازي العامل
CRON_CONCURRENCY=5              # توازي المجدول
STREAM_REFRESH_WINDOW_SECONDS=1800  # نافذة تحديث الروابط
```

### Docker

```dockerfile
FROM node:20-slim
# تثبيت Playwright dependencies
# npm install && npm run build
# CMD ["node", "server.js"]
```

---

## أداء

- **Static Generation:** صفحات ثابتة للسرعة
- **Dynamic Routes:** صفحات ديناميكية للمحتوى المتغير
- **Code Splitting:** تحميل الأكواد حسب الحاجة
- **Image Optimization:** غير مفعّل (لـ Supabase Storage)
- **P2P:** توصيل فيديو عبر WebTorrent (0% server bandwidth)
- **Service Worker:** تخزين مؤقت + تزوير Referer

---

## الإحصائيات

| البند | العدد |
|-------|-------|
| صفحات | 21 |
| مكونات React | 50+ |
| مكونات UI | 30+ |
| API Routes | 6 |
| جداول قاعدة البيانات | 15 |
| دوال قاعدة البيانات | 12 |
| فهارس | 25+ |
| سياسات RLS | 40+ |

---

## ملاحظات للمشتري

1. **قاعدة البيانات:** شغّل `supabase_production_schema.sql` في SQL Editor
2. **المفتاح:** تأكد من صحة `NEXT_PUBLIC_SUPABASE_ANON_KEY`
3. **الhiro:** أنشئ حساب مدير بعد النشر
4. **المحتوى:** أضف الأفلام والمسلسلات من لوحة التحكم
5. **البيئة:** حط متغيرات البيئة في Render

---

*آخر تحديث: سبتمبر 2026*
